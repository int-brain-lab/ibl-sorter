services:
  test:
    image: int-brain-lab/pykilosort:latest
    entrypoint: /bin/bash -c "nvidia-smi && python -c 'import torch; print(torch.cuda.is_available())'"
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
